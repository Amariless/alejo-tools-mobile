# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Scripts locales de release/upload manual (release.ps1, upload.ps1, etc.):
# NUNCA versionar estos -- históricamente terminaron con un token de GitHub
# hardcodeado adentro (bug real, corregido a mano una vez, ver historial).
# El flujo de release real de este proyecto usa curl + git credential fill
# desde la sesión de IA, no estos scripts locales.
release.ps1
upload.ps1
